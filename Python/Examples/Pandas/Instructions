Need Python installed then in order to install Pandas is required
    
    pip install pandas

for converting file  into excel it's necessary

    pip install openpyxl

for printing graph 

    pip install mathplot

for regression line
    
    pip install scikit-learn